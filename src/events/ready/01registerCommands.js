const { testServer } = require('../../configs/config.json');
const registerCommands = require('../../helper/registerCommands');

module.exports = async (client) => {
    // probably gonna use this for global commands instead
    // await registerCommands(client, testServer);
};