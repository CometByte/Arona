require('dotenv').config();

module.exports = async (client) => {

    console.log("📋 birthday-reminder executed!");
    // Get connection string
    const connectionString = process.env.CONNECTION_STRING;
    // Check MongoDB connection

    // Get reminder and birthday channel id

    // Check if reminder channel is not null
    // Query the upcoming birthdays by 5 days starting tomorrow
    // Display the birthdays today in the birthday channel
    
    // Check if birthday channel is not null
    // Query the students with birthday today
    // Display the upcoming birthdays in the reminder channel
};